This module fails the tests intentionally. If you don't know how to bypass,
the current development stage is not meant for you.

Other than that, Transporter is fairly straightforward in terms of
installation.